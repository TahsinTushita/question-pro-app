import { Link } from "react-router-dom";

const CreateForm = () => {
  return (
    <section>
      <h1>Create Form</h1>
      <Link to="/todos">todos</Link>
    </section>
  );
};

export default CreateForm;
